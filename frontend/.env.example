# URL of the deployed Railway backend.
# Leave blank (or omit entirely) to use the built-in static data instead.
# Set this in Vercel's dashboard under: Project → Settings → Environment Variables
VITE_API_URL=https://your-service.up.railway.app
